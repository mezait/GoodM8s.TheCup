@model GoodM8s.Core.ViewModels.DynamicIndexViewModel
@{
    Layout.Title = T("Scores").ToString();
}
<fieldset>
    @using (Html.BeginFormAntiForgeryPost())
    {
        @Html.ValidationSummary()
        <div class="manage">@Html.ActionLink(T("Add a new score").ToString(), "Create", new { }, new { @class = "button primaryAction" })</div>
        <table class="items">
            <thead>
                <tr>
                    <th scope="col">@T("Cup")
                    </th>
                    <th scope="col">@T("Event")
                    </th>
                    <th scope="col">@T("Actions")
                    </th>
                </tr>
            </thead>
            @foreach (var score in Model.List)
            {
                <tr>
                    <td>@score.Cup
                    </td>
                    <td>@score.Event
                    </td>
                    <td>
                        <div>
                            @Html.ActionLink(T("Edit").ToString(), "Edit", new { score.Id }) |
                            @Html.ActionLink(T("Delete").ToString(), "Delete", new { score.Id }, new { itemprop = "RemoveUrl UnsafeUrl" })
                        </div>
                    </td>
                </tr>
            }
        </table>
    }
    @Display(Model.Pager)
</fieldset>
